# Read Length and width as floating-point numbers
length = float(input())
width = float(input())

# calculate area
area = length * width

# Print area formatted to 2 decimal places
print(f"{area:.2f}")